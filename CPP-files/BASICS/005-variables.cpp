#include <iostream>

int main()
{
    // int a, double b; // wrong (compiler error)

    int a; double b; // correct (but not recommended)

    // correct and recommended (easier to read)
    int c;
    double d;

   
    // Data is any information that can be moved, processed, or stored by a computer.

    /* A value is a letter (e.g. a), number (e.g. 5), text (e.g. Hello), or instance of 
    some other useful concept that can be represented as data.*/



    //  Variables have an identifier, a type, and a value
    
    // An identifier is the name that a variable is accessed by.


    return 0;
}
